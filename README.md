# Arcane documentation

This repository contains the Mintlify documentation scaffold for Arcane.

The navigation in `docs.json` follows the approved **Start Here → Products → Support** information architecture.

## Local checks

```bash
npm install --global mint
mint validate
mint broken-links --check-anchors
```
